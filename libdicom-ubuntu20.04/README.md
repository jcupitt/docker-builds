# libdicom on ubuntu 20.04

```
docker pull ubuntu:focal
git clone git@github.com:hackermd/libdicom.git
docker build -t libdicom-ubuntu20.04 .ssh-keyscan github.org
docker run -it --rm libdicom-ubuntu20.04 
```
